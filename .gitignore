pipeline/models/
pipeline/third_party/

# data/ 下只让几 KB 的元数据 JSON 进 git（PLY / SOG / 帧序列 / 编译日志都不进）。
# 注意写法：一旦用 `pipeline/data/` 整体排除目录，git 就不再遍历其中内容，
# 后面的 `!...*.json` 会完全失效 —— 所以这里排除的是目录"里的内容"（/**），
# 再逐级把需要的目录和文件放回来。
pipeline/data/**
!pipeline/data/ply/
!pipeline/data/sog/
!pipeline/data/ply/*.json
!pipeline/data/sog/*.json

# 原始素材：视频动辄几十上百 MB，和 data/ 一样不进 git（§4）
media/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Node
node_modules/
web/dist/
# tsc -b 的增量编译缓存
*.tsbuildinfo

# web 的 splat 资产：二进制一律不进 git（butterfly.* 是一次性下载的官方参照，
# riverview.sog 台式机自己跑 04_export.sh 就能生成）。同名 .json 只有几 KB，
# 是前端定相机要用的，必须进 git —— 写法同 pipeline/data，先排内容再放回来。
web/public/splats/**
!web/public/splats/*.json

# 编译日志
*.build.log

# 编辑器/系统
.DS_Store
.idea/
.vscode/
